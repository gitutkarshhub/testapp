name: Test Android Build

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true  # Enforces GitHub's June 2026 runtime requirement

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Create Mock Build
        run: npm run build

      - name: Set up Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Initialize Android Shell
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap add android || echo "Android folder already exists"
          npx cap sync android

      - name: Compile Test APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Export Output File
        uses: actions/upload-artifact@v4
        with:
          name: compiled-test-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
